Integration tests for the data processing pipeline using `fs_prep`, `proc.attr.hydfab` and `fs_algo` packages.

The current version of input data to run these tests is inside testdata_20250901.zip.
`# TODO create a publicly-available location for these test data` 