This is a test case workflow on unsupervised clustering.
It uses parameter value data from the 2026 June calibration experiments run on URSA,
based on a subset of ngenCERF locations (n=298), where n=233 yielded acceptable
results for further regionalization consideration.