This subdirectory contains example workflows using the `hfatlas`-created watershed attribute data that serve as predictors.

Note that datasets may not be publicly available to run these workflows. The intent is to demonstrate how RaFTS may be configured for various types of workflows.
