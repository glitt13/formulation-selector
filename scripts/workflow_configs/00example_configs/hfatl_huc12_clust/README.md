This is a demonstrative workflow of cluster-based regionalization to HUC12 basins.
This workflow specifically uses the `fs_agg_hfatl_basin.py` attribute preparation 
that aggregates the attributes of donor watersheds by calibration basin.
This workflow uses pre-aggregated watershed attribute data representing
 the predicted locations (HUC12 basins).